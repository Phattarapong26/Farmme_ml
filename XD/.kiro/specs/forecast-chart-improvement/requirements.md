# Requirements Document

## Introduction

ปรับปรุงกราฟเส้นในหน้า Forecast Chart (RealForecastChart component) ให้มีความชัดเจน อ่านง่าย และเข้าใจได้ง่ายขึ้นสำหรับผู้ใช้งาน โดยเน้นการปรับปรุงด้าน UI/UX ของกราฟเส้นที่แสดงข้อมูลราคาในอดีตและราคาพยากรณ์

## Glossary

- **Forecast Chart**: กราฟแสดงการพยากรณ์ราคาพืชที่ใช้ Recharts library
- **Historical Data Line**: เส้นกราฟที่แสดงราคาพืชในอดีต (สีน้ำเงิน)
- **Predicted Data Line**: เส้นกราฟที่แสดงราคาพยากรณ์จาก ML Model (สีส้ม เส้นประ)
- **Chart Readability**: ความสามารถในการอ่านและเข้าใจข้อมูลจากกราฟได้ง่าย
- **Data Point**: จุดข้อมูลบนกราฟ
- **Tooltip**: กล่องข้อมูลที่แสดงเมื่อ hover เหนือกราฟ
- **Legend**: คำอธิบายสัญลักษณ์ของเส้นกราฟ

## Requirements

### Requirement 1

**User Story:** ในฐานะผู้ใช้งาน ฉันต้องการเห็นเส้นกราฟที่มีความหนาและสีที่เหมาะสม เพื่อให้สามารถแยกแยะระหว่างข้อมูลจริงและข้อมูลพยากรณ์ได้ชัดเจน

#### Acceptance Criteria

1. WHEN ผู้ใช้ดูกราฟ, THE Forecast Chart SHALL แสดงเส้น Historical Data Line ด้วยความหนา 3 pixels และสีน้ำเงินเข้ม (#2563eb)
2. WHEN ผู้ใช้ดูกราฟ, THE Forecast Chart SHALL แสดงเส้น Predicted Data Line ด้วยความหนา 3 pixels สีส้ม (#f97316) และเป็นเส้นประ
3. WHEN ผู้ใช้ดูกราฟ, THE Forecast Chart SHALL แสดง Data Point ขนาด 5 pixels สำหรับทั้งสองเส้น
4. WHEN ผู้ใช้ดูกราฟ, THE Forecast Chart SHALL ใช้สีที่มี contrast สูงเพื่อให้มองเห็นได้ชัดเจน

### Requirement 2

**User Story:** ในฐานะผู้ใช้งาน ฉันต้องการเห็นแกน X และแกน Y ที่อ่านง่าย เพื่อให้เข้าใจข้อมูลวันที่และราคาได้ชัดเจน

#### Acceptance Criteria

1. WHEN กราฟแสดงข้อมูล 7 วัน, THE Forecast Chart SHALL แสดงวันที่ทุกวันบนแกน X โดยไม่มีการหมุนข้อความ
2. WHEN กราฟแสดงข้อมูล 30-180 วัน, THE Forecast Chart SHALL แสดงวันที่แบบเว้นระยะที่เหมาะสม และหมุนข้อความ -45 องศา
3. WHEN กราฟแสดงข้อมูล, THE Forecast Chart SHALL แสดงป้ายกำกับแกน Y เป็น "ราคา (บาท/กก.)" ในแนวตั้ง
4. WHEN กราฟแสดงข้อมูล, THE Forecast Chart SHALL ใช้ตัวอักษรขนาด 12-14 pixels สำหรับแกนทั้งสอง
5. WHEN กราฟแสดงข้อมูล, THE Forecast Chart SHALL แสดงเส้นตาราง (grid) สีเทาอ่อนเพื่อช่วยในการอ่านค่า

### Requirement 3

**User Story:** ในฐานะผู้ใช้งาน ฉันต้องการ Tooltip ที่แสดงข้อมูลครบถ้วนและอ่านง่าย เพื่อให้เข้าใจรายละเอียดของแต่ละจุดข้อมูล

#### Acceptance Criteria

1. WHEN ผู้ใช้ hover เหนือ Data Point, THE Forecast Chart SHALL แสดง Tooltip พร้อมวันที่แบบเต็ม (วัน/เดือน/ปี)
2. WHEN ผู้ใช้ hover เหนือจุดข้อมูลจริง, THE Forecast Chart SHALL แสดงราคาจริงพร้อมไอคอน "📊"
3. WHEN ผู้ใช้ hover เหนือจุดข้อมูลพยากรณ์, THE Forecast Chart SHALL แสดงราคาพยากรณ์พร้อมไอคอน "🔮"
4. WHEN ผู้ใช้ hover เหนือจุดเชื่อมต่อ, THE Forecast Chart SHALL แสดงทั้งราคาจริงและราคาพยากรณ์
5. WHEN Tooltip แสดงผล, THE Forecast Chart SHALL ใช้พื้นหลังสีขาวพร้อม shadow และ border radius 8 pixels

### Requirement 4

**User Story:** ในฐานะผู้ใช้งาน ฉันต้องการ Legend ที่ชัดเจนและอยู่ในตำแหน่งที่เหมาะสม เพื่อให้เข้าใจความหมายของแต่ละเส้นกราฟ

#### Acceptance Criteria

1. WHEN กราฟแสดงผล, THE Forecast Chart SHALL แสดง Legend ด้านบนของกราฟ
2. WHEN กราฟแสดงผล, THE Forecast Chart SHALL ใช้ไอคอนและข้อความที่ชัดเจนใน Legend
3. WHEN กราฟแสดงผล, THE Forecast Chart SHALL แสดง "📊 ราคาในอดีต" สำหรับ Historical Data Line
4. WHEN กราฟแสดงผล, THE Forecast Chart SHALL แสดง "🔮 ราคาพยากรณ์ (ML)" สำหรับ Predicted Data Line

### Requirement 5

**User Story:** ในฐานะผู้ใช้งาน ฉันต้องการเห็นการเชื่อมต่อที่ราบรื่นระหว่างข้อมูลจริงและข้อมูลพยากรณ์ เพื่อให้เข้าใจความต่อเนื่องของข้อมูล

#### Acceptance Criteria

1. WHEN กราฟมีทั้งข้อมูลจริงและพยากรณ์, THE Forecast Chart SHALL สร้างจุดเชื่อมต่อ (bridge point) ระหว่างเส้นทั้งสอง
2. WHEN จุดเชื่อมต่อแสดงผล, THE Forecast Chart SHALL ใช้ราคาล่าสุดจากข้อมูลจริงเป็นจุดเริ่มต้นของการพยากรณ์
3. WHEN เส้นกราฟแสดงผล, THE Forecast Chart SHALL ใช้ curve type "monotone" เพื่อให้เส้นดูราบรื่น

### Requirement 6

**User Story:** ในฐานะผู้ใช้งาน ฉันต้องการเห็นกราฟที่ responsive และแสดงผลได้ดีในทุกขนาดหน้าจอ เพื่อให้สามารถใช้งานได้บนอุปกรณ์ต่างๆ

#### Acceptance Criteria

1. WHEN กราฟแสดงผลบนหน้าจอขนาดต่างๆ, THE Forecast Chart SHALL ปรับขนาดอัตโนมัติให้เหมาะสมกับพื้นที่
2. WHEN กราฟแสดงผลบนมือถือ, THE Forecast Chart SHALL ลดขนาดตัวอักษรและปรับมุมของแกน X ให้เหมาะสม
3. WHEN กราฟแสดงผล, THE Forecast Chart SHALL มีความสูงขั้นต่ำ 384 pixels (h-96)
